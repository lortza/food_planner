<% content_for(:title, 'Add a Recipe') %>

<p><%= link_to MaterialIcon.new(icon: :arrow_left).render + 'Recipes', recipes_path %></p>

<h1>Create a Recipe</h1>

<%= render partial: 'form', locals: { recipe: @recipe, tags: current_user.tags.by_name } %>
