<footer class='footer--delete'>
  <%= link_to "Delete #{object.class.name}",  object,  method: :delete, data: { confirm: 'Are you sure? This action cannot be undone.' }, class: "#{button_classes('danger')} mb-5"  %>
</footer>
