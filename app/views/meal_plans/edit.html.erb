<%= link_to 'Cancel', meal_plans_path, class: 'btn btn-outline-primary float-right' %>

<h1>Edit the Plan for <%= @meal_plan.start_date.to_s(:long) %> </h1>

<%= render 'form', meal_plan: @meal_plan %>

<%= render partial: 'shared/delete_footer', locals: { object: @meal_plan } %>
